import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#008000", // Customize your primary color
    },
    secondary: {
      main: "#ff0000", // Customize your secondary color
    },
  },
});

export default theme;
